# 路线图

本文件记录**尚未完成**的工作与明确暂缓的方向。已完成的能力见 [README](README.md) 与 [docs/PRODUCT.md](docs/PRODUCT.md) §6。

优先级不构成承诺，实际顺序取决于 DSH 上游契约的演进与本项目维护带宽。欢迎在 Issue 中讨论或直接提 PR。

## 近期（已有接口或明确入口，可直接动手）

### 1. 宿主动作缺失降级的自动化测试

**已完成（2026-09-11）。** 渲染级测试已落地：`test/render-smoke.test.js` 用真实 `react-dom/server` 渲染初始状态（不引入 jsdom，DOM 缺失面由 `test/helpers/dom-stub.js` 补齐），对五处守卫逐一断言——

| 文件 | 自动化覆盖 |
| --- | --- |
| `src/research-workbench.js` | SSR：降级提示存在、写入/发送按钮带 `disabled`；对照组（提供 inputActions）恢复可用 |
| `src/composer-overlay.js` | SSR：关闭态渲染不抛错；接线断言：`hasDraftAction` 守卫与 `setNotice` 降级存在 |
| `src/research-vault.js` | SSR：完整渲染不抛错；接线断言：`setDraft` 守卫存在（降级走异步 `setError`，初始渲染不可见） |
| `src/database-query-panel.js` | SSR：完整渲染不抛错；接线断言：`canWrite` 守卫与写入前提前返回存在 |
| `src/research-evidence-vault.js` | SSR：写入按钮带 `disabled`；接线断言：`canWrite` 与选择数联合守卫存在 |

**边界（不掩饰）**：SSR 只覆盖初始渲染态。「点击后是否真的不写入」属于交互，需要真实 DOM 事件运行时（jsdom 或浏览器级 harness）才能构造，目前仍由纯逻辑决策测试（`planCitationWrite` 三态）+ 源码接线断言 + 真实 profile 现场核对（W2/W3）三层共同守护。若要继续推进交互级覆盖，需在 CI 引入 jsdom 并把现有桩换成可触事件的 DOM 实现——这是本项后续的演进方向，不是缺口。

**为什么重要**：这是插件与宿主契约之间最主要的降级路径，也是真实 profile 烟测中唯一无法从外部构造的项。

### 2. 工作台左列 sticky

**现状**：分区① 左列（资源列表）非 sticky。右列详情可达约 1044px，左列仅约 415px 且随页面滚走——滚到详情底部的出口按钮时，列表已完全滑出视口。

**要做**：给左列加 `position: sticky` + 视口内高度约束。

**注意**：同一分区内的祖先 `overflow` 会夺走 sticky 参照系，参考[架构文档 §2.3](docs/ARCHITECTURE.md) 中方法工坊的两条 vendor 解绑记录。

### 3. 证据图谱连线的方向表达（已完成 · 随 §4d 落地）

**已完成（2026-09-11）。** 原状是连线固定「从左缘连到右缘」，边方向本身逆向时（如 `workflow → resource`，工作流列在资源列右侧）线段会穿过节点、产生穿越感；当时没有数据会触发该路径。§4d 把已保存证据接入图谱后产生了该方向的边，本项随之落地：箭头按两端节点的**实际水平位置**选锚点——正向走「右缘 → 左缘」，逆向走「左缘 → 右缘」，不再固定「左进右出」。因锚点自适应会让箭头指向不再自明，图谱说明显式标注「箭头表示关系方向」。

实现见 `src/research-evidence-graph.js` 的 `edgePoints()`；真实验收见 [MANUAL-QA](docs/MANUAL-QA.md) 的 G4。

> 编号未回填：§4 及其下的 4a–4d 切片编号被多份文档与源码注释引用，重编号会让这些引用失效。本节保留原位置与编号，只更新状态。

### 3.1 专用科研证据 Viewer

**已完成（2026-09-11）。** 分区④在既有关系图（节点、边、方向性锚点、链路流动动画）之上补齐了面向科研证据的探索能力，全部在插件内实现、未接入第三方渲染器：

- **确定性分层布局与端口路由**（`layoutEvidenceGraph` / `routeEvidenceEdges`，纯逻辑）：按节点 kind 分列、同列按 id 字典序，新增节点不让已有节点跳位；边按两端实际水平位置选锚点，同侧多条边按端口展开，贝塞尔连线避免穿越；
- **缩放、平移、复位**：Ctrl / ⌘ 滚轮缩放（限幅 40%–250%）与按钮缩放以视口中心为锚，拖拽平移带边界钳制（图拖不丢），一键复位；
- **迷你地图**：右下角常驻全图缩略，点击跳转，视口框实时联动；
- **聚焦 / 上游 / 下游 / 两点路径四种范围模式**：BFS 邻域与无向最短路均为纯逻辑（`evidenceNeighborhood` / `findEvidencePath`），路径模式忽略箭头方向——用户问的是「这两个东西怎么连上的」；非范围内节点降透明度而非移除，保留参照系；
- **键盘可达**：节点是 `role="button"` + `tabIndex`，Enter / 空格触发聚焦；
- **`prefers-reduced-motion` 静态降级**：系统关闭动画时链路流动自动停止；窄屏（<880px）随统一容器单列收敛，画布不再有固定最小宽度；
- **URL 只保存视图状态**：焦点、范围模式、路径两端与缩放经 `encodeGraphView` 写入 hash，**不编码任何证据内容**——链接会被转发，标题与标识符都不该进去；
- **脱敏静态导出（用户显式触发）**：SVG 与离线可打开的 HTML 双格式；导出弹窗与导出文件正文都写明元数据范围（仅含节点标题、来源库、稳定标识符、核验状态与关系，不含笔记、全文、检索词、附件或输入框草稿）；导出物把当前主题解析为真实色值，脱离 CSS 变量后不再依赖宿主环境。

**自动化覆盖**：布局确定性/稳定性/分列规则、路由锚点、邻域/路径语义、缩放限幅、URL 编解码往返（含「内容不进链接」）共 17 项纯逻辑测试（`test/evidence-graph.test.js`）。

**剩余验收**：真实 profile 上以至少一条「数据库 → 查询 → 来源 → 已保存证据」的链路完成聚焦、上下游、路径、缩放与迷你地图操作；窄屏可读；导出物脱敏且离线可打开。导出与复制链接时的元数据范围提示已在实现中落地，现场核对即可。该项已登记到 [MANUAL-QA](docs/MANUAL-QA.md) 待验清单（V1–V4）。

**同批交付的另两项可视化能力（不在本节的遗留清单内）**：工作台「组装回放」（`src/route-replay.js` + archify viewer 弹窗）与「研究结果解释图生成」工作流 + 出图工具链（`scripts/render-diagrams.mjs` / `validate-diagrams.mjs`）已随本次交付落地，细节见 [CHANGELOG](CHANGELOG.md)。本节的分区④ Viewer 只覆盖证据图谱，**不是**本仓库可视化能力的全部——按路线图判断可视化进度时不要只看本节。

## 中期（需要上游支持或较大改动）

### 4. 研究资产库：本地证据库

**目标**：在沉淀层「研究资产库」中维护与「灵感资产」并列的「证据库」子模块，沉淀用户**明确保存且可追溯**的论文、数据集、临床试验和数据库记录，供后续研究任务检索与复用。

**产品定位**：不新增第五个并列工作台分区。沉淀层在 4a–4b 落地后已更名为「研究资产库」，内含「灵感资产 / 证据库」两个子模块：灵感资产回答“想过什么”，证据库回答“依据什么”。关系可视化由既有的独立第四分区「研究证据图谱」承担，它是只读的，不替代证据条目的管理与核验。

**隐私与数据边界**（不可协商，适用于下面每一个切片）：

- 仅持久化用户确认的元数据与主动写下的笔记；默认不保存原始 API 响应、检索词、完整全文、附件或未经用户确认的模型摘要；
- 本地持久化改用 IndexedDB，而不是无上限扩张 `localStorage`；提供按项目导出、导入与彻底删除；
- 不在草稿增强、工作流启动或 Agent 调用时静默注入历史证据；所有复用必须经历“检索 → 预览 → 用户勾选 → 写入 Prompt”；
- 写入 Prompt 时不得将本地条目表述为已证实结论，必须保留来源与人工核验责任。

**交付切片**：按依赖顺序拆为四片，**每片独立验收、独立上线**。四片截至 2026-09-11 **已全部交付**，各片的范围与验收结论保留如下（逐片细节见 [CHANGELOG](CHANGELOG.md)，现场验收项见 [MANUAL-QA](docs/MANUAL-QA.md)）。

**已完成：4a 保存与条目。** 条目字段（标题 / 来源数据库 / 稳定标识符 / 原始链接 / 保存时间 / 项目 / 标签 / 保存原因 / 用户笔记 / 核验状态）已落地，持久化直接建 IndexedDB 最小 schema；保存入口在查询结果条目上逐条触发，列表在沉淀层分区的「证据库」子模块。细节见 [CHANGELOG](CHANGELOG.md)。

**已完成：4b 持久化与项目隔离。** 跨刷新持久化走 IndexedDB；按项目隔离与切换，当前项目作为工作上下文跨会话保留；同一稳定标识符在**同项目内**去重（跨项目不去重——项目隔离优先），命中重复时交给用户裁决「覆盖已有 / 仍然另存」，不自动合并；单条删除与按项目彻底删除；按项目导出 JSON 备份与增量导入（已存在条目跳过，恢复是补齐不是回滚）。细节见 [CHANGELOG](CHANGELOG.md)。

**已完成：4c 勾选写入 Prompt。** 证据库子模块内勾选条目后给出引用块预览，「写入 Prompt（N）」把带来源链接与「尚未经逐条核验、不得据此直接断言结论」边界的引用块写入当前会话输入框；宿主未提供输入框操作时按钮禁用、引用块转为手工复制。选择以**全部证据条目**为基准，不随筛选条件变化被静默撤销。注入与否由纯函数 `planCitationWrite()` 决策（`empty / unsupported / write` 三态，仅 `write` 允许调用 `setDraft`），本节原定的两项验收——「未选择不注入」回归测试与写入边界断言——均已落地。**真实 profile 验收（[MANUAL-QA](docs/MANUAL-QA.md) 的 W1–W3）已于 2026-09-11 通过**：勾选后按钮可用、未选择时按钮点不动、写入不自动发送且提示条数与实际注入一致。细节见 [CHANGELOG](CHANGELOG.md)。

**已完成：4d 图谱接入。** 已保存证据作为独立节点接入图谱，节点上展示来源库、稳定标识符与核验状态，**不携带笔记、全文或检索词**；证据按来源库连到同库资源节点，并按 URL 或稳定标识符匹配本会话查询来源，工作流仍经资源节点形成可追溯链路。原先「连线方向表达」的遗留问题随之解决——箭头按两端节点的实际水平位置选择左右锚点，逆向关系不再固定「左进右出」。**真实 profile 验收已于 2026-09-11 通过**：测试证据与 PubMed 资源共呈 2 个节点 / 1 条关系，图谱说明明确标注箭头含义。细节见 [CHANGELOG](CHANGELOG.md)。

**整体验收**：四片已全部落地，其中 4c、4d 已在真实 DSH profile 上验收（见 [MANUAL-QA](docs/MANUAL-QA.md) 的 W1–W3 与 G1–G4）。

### 5. Memory Center 项目记忆检索

**目标**：让 Agent 能引用项目历史决策与既有证据。

**硬约束**（不可协商）：检索 → 显示来源与文本摘要 → 用户选择 → 组装 Prompt。**禁止静默注入。**

**与 §4 的边界**：两者流程完全相同（检索 → 预览 → 用户勾选 → 写入 Prompt），差别只在内容归属——可追溯到外部稳定标识符（DOI / PMID / NCT / 数据集 ID）的来源归证据库，项目内部的决策与上下文归 Memory Center。这条判据需要在动手前定死，否则会做成两个互相抢内容的收件箱。

**现状**：上下文桥已预留接口，检索路由尚未接入 Research Kit 的依赖范围。

### 6. 宿主能力探测

**目标**：按 DSH 实际已连接的 MCP / 工具标注数据源与技能，而不是让用户自己判断。

**要做**：新增独立的 `HostCapabilitiesProvider`，返回**已经验证过**的能力（例如 `{ web: true, mcpServers: ['pubmed'] }`）。目录卡片与发送前检查只读该 provider，**不得根据资源名称推断数据库已可用**。

**门槛**：只有实现了真实探测并覆盖自动化测试后，条目才允许从 `requires-mcp` 升级为 `available-in-host`。

**与 `available-in-plugin` 的区别**：本项解决的是「宿主给了什么」，与插件自身是否已实现直查无关。11 个条目已因插件内置适配器标为 `available-in-plugin`（由目录与实现的契约校验守护），不构成本项的交付，也不因此免除能力探测。

### 7. 发送前自动增强

**目标**：在用户点发送时自动对草稿做一次增强。

**阻塞项**：依赖宿主下发「发送当前草稿」钩子（onSubmitDraft），当前 DSH 槽位契约未提供。上游支持前不做。

### 8. 目录内容扩充（工作流）

**现状**：工作流目录已按流程族分片维护于 `catalog/workflows/`（`index.js` 聚合），共 317 条、24 个类目——临床研究 19 / 数据分析 18 / 论文与手稿 17 / 化学 18 / 药物发现 15 / 蛋白组与结构生物学 13 / 材料科学 12 / 文献研究 16 / 生物信息学 18 / 基因组学 14 / 可视化与图表 13 / 科研传播 10 / 基金申请 10 / 细胞生物学 9 / 作物遗传育种 8 / 研究设计 5 / 机器学习 15 / 生态学 14 / 物理学 14 / 神经科学 12 / 社会科学 12 / 数学 12 / 工程学 12 / 天文与空间科学 11。原 8 个预留空分片（生态、神经科学、物理、天文、社会科学、数学、机器学习、工程）已全部由 K-Dense 上游条目中文化迁入填满。

**要做**：向现有类目继续扩充工作流（上游非金融存量中已有类目尚有约 10 条散落缺口，按 id 对账：基金申请 -2 / 基因组学 -3 / 细胞生物学 -4 / 药物发现 -1）。每条新增工作流至少做一次「最小材料」人工走查——检查模型是否被引导为虚构事实、过度承诺或遗漏不确定性。

### 9. 科研模式领域预设（已完成）

**已完成。** `src/research-workbench.js` 的 `SCIENCE_MODE_PRESETS` 现有 **6 个**预设：通用研究（general）/ 文献与论文（literature）/ 生物信息学（bioinformatics）/ 作物遗传育种（cropBreeding）/ 临床与人群研究（clinical）/ 数据分析与可视化（dataVisualization），与 [README](README.md)、[架构文档 §3.0b](docs/ARCHITECTURE.md)、[开发指南](docs/DEVELOPMENT.md) 的口径一致。原文称该常量仅有三个预设（基因遗传 / 临床队列 / 通用科研），那是合并前的状态，已失效——按本节判断进度会以为要做的事其实早已做完。

**扩展方式**：新增领域预设仍只需在该常量中加条目（技能组合 + 纪律段 + 标签）。

**注意**：本节与 §8 是不同文件、不同层——工作流是目录资产，领域预设是附加到 Prompt 上的科研纪律段，两者不可互相替代。

> 编号未回填：本节保留原编号与位置，只更新状态（同 §3 的处理）。

### 10. vendored 工件的可维护化

**现状**：`vendor/promptkit-embed.js` 是 SHA 锁定的生成工件，约 637 KB，已进入 bundle。仓库现有 **3 个** vendored 工件——promptkit 工件 + `vendor/archify/template.html`（756 KiB，整段注入为全局字符串）+ `vendor/archify/i18n.mjs`（参与拼接），清单见 `vendor/vendor-manifest.json`；三者合计约占构建产物 2.3 MB 中的 1.36 MB。

**动机**：主要不是体积——对本地插件而言 637 KB 不是主要成本——而是它**不可手改、不可逐行 review，且 SHA 锁定意味着任何升级都只能整体替换**。目标是逐步换成可维护、可审查的源码子模块。

**要做**：视维护需要逐步替换。

**门槛**：替换后的模块必须在真实 DSH profile 上通过 [MANUAL-QA.md](docs/MANUAL-QA.md) 的完整清单。

## 明确暂缓（不做或暂不做）

以下方向**刻意不做**，理由是它们会重复 DSH 已承担的能力，并带来权限、保密、成本与状态同步问题：

- 自动并行子 Agent；
- 长任务调度与后台恢复；
- 自动化事实核查结论；
- 独立沙箱、成本限额与计算资源管理；
- 对原始科研数据的自动修改；
- 自带模型路由、自持 API Key 或独立 Agent 运行时。

若你认为其中某项对你有价值，欢迎开 Issue 说明使用场景——"暂缓"不等于"永不"，但需要有能说服人的理由。
